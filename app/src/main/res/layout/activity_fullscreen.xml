<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorFullscreenBackground"
    tools:context="ru.illarionovroman.yandexmobilizationhomework.ui.activity.FullscreenActivity">

    <ImageView
        android:id="@+id/ivCloseFullscreen"
        android:layout_width="@dimen/image_size_close_fullscreen"
        android:layout_height="@dimen/image_size_close_fullscreen"
        android:src="@drawable/ic_cross_white"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_margin="@dimen/margin_full"/>

    <TextView
        android:id="@+id/tvFullscreenText"
        style="@style/TextFullscreen"
        android:keepScreenOn="true" />

</RelativeLayout>
